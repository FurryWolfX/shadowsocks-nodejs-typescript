export interface Args {
  local_port?: string;
  server_port?: string;
  server?: string;
  password?: string;
  config_file?: string;
  method?: string;
  local_address?: string;
  timeout?: number;
  verbose?: boolean;
}
